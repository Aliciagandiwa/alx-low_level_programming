#include "main.h"

/**
 * mul - Multiply two intergers
 * @x: The first digit to be multiply
 * @y: The second digit to be multiply with first digit
 * Return: The result
 */
int mul(int x, int y)
{
	return (x * y);
}
