#include "main.h"

/**
 * _isupper - Checks id letter is upper
 * @x: The number to be checked
 * Reurn: 1 for upper letter or 0 for otherwise
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
