#include "main.h"

/**
 * _issupper - Checks if letter is an uppercase
 * @x: The number to be checked
 * Reurn: 1 for upper letter or 0 for otherwise
 */

int _issupper(int x)

{
	if(x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
