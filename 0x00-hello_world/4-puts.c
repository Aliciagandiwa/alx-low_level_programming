#!/bin/bash
#include <stdio.h>

/**
 * main - void
 * 
 * return: return 0 if successful or 1 if failed
 */

int main(void)
{
         puts("\"Programming is like building a multilingual puzzle);
	 retun (0);

}
